<%#
# New

This view is the template for the "new resource" page.
It displays a header, and then renders the `_form` partial
to do the heavy lifting.

## Local variables:

- `page`:
  An instance of [Administrate::Page::Form][1].
  Contains helper methods to help display a form,
  and knows which attributes should be displayed in the resource's form.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Page/Form
%>

<% content_for(:title) do %>
  Invited User
<% end %>

<header class="main-content__header" role="banner">
  <h1 class="main-content__page-title">
    <%= content_for(:title) %>
  </h1>

  <div>
    <%= link_to t("administrate.actions.back"), :back, class: "button" %>
  </div>
</header>

<section class="main-content__body">
  <%= form_for @user, url: admin_redeem_invite_path do |form| %>
    <div class="field-unit field-unit--string field-unit--optional">
      <%= form.label :email, class: "field-unit__label" %>
      <%= form.text_field :email, type: 'email', class: "field-unit__field" %>
    </div>

    <div class="field-unit field-unit--password field-unit--optional">
      <%= form.label :password, class: "field-unit__label" %>
      <%= form.password_field :password, class: "field-unit__field" %>
    </div>

    <div class="form-actions">
      <%= form.submit %>
    </div>
  <% end %>
</section>
